'use strict';

import angular from 'angular';
const swagyoselfApp = angular.module('swagyoselfApp');

swagyoselfApp.factory('playaService', ['$log', 'mapService', playaService]);

function playaService($log, mapService) {
}
